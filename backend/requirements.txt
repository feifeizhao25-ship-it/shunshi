# 顺时后端 Python 依赖
# Core
fastapi
uvicorn[standard]
pydantic
aiohttp
httpx

# Auth security
bcrypt>=4.0.0
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Cache
redis>=5.0.0

# Payment - 支付宝 (CN)
python-alipay-sdk>=3.0.0

# Payment - Stripe (GL)
stripe>=10.0.0

# Push notifications (可选 - 无凭据时自动降级为 mock)
firebase-admin>=6.0.0
apns2>=0.7.0

# Monitoring
prometheus_client>=0.20.0
